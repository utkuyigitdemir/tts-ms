# TTS-MS Environment Configuration
# Copy this file to .env and adjust values as needed

# TTS Engine Selection
# Options: piper, f5tts, cosyvoice, styletts2, chatterbox, legacy
TTS_MODEL_TYPE=piper

# Logging
# 1=MINIMAL, 2=NORMAL (default), 3=VERBOSE, 4=DEBUG
TTS_MS_LOG_LEVEL=2

# Disable colored console output (useful for log aggregators)
# TTS_MS_NO_COLOR=1

# Resource Monitoring
TTS_MS_RESOURCES_ENABLED=1
TTS_MS_RESOURCES_PER_STAGE=1
TTS_MS_RESOURCES_SUMMARY=1

# Model Cache Directory
# TTS_HOME=/path/to/cache

# Skip warmup at startup (testing only)
# TTS_MS_SKIP_WARMUP=1
